<UserControl x:Class="BrainBurst.Presentation.Views.TestsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrainBurst.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1180"
             Background="White">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <TextBlock Text="Мої тести" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <Grid Margin="0,0,0,20">
                <TextBox Style="{StaticResource SearchBox}" Tag="Пошук..." Text=""/>
                <TextBlock Text="🔍" FontSize="20" 
                           HorizontalAlignment="Right" 
                           VerticalAlignment="Center" 
                           Margin="0,0,15,0"
                           Foreground="#888"/>
            </Grid>

            <TextBlock Text="Продовжити вивчення" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>
            <WrapPanel>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="дискретна математика" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 27" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="математичний аналіз" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 106" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <TextBlock Text="Нещодавні" FontSize="22" FontWeight="Bold" Margin="0,20,0,10"/>
            <WrapPanel>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="психологія" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 244" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="англійська мова" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 190" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="програмування" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 32" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource DeckCard}" MouseLeftButtonDown="Test_Click">
                    <StackPanel>
                        <TextBlock Text="теорія ігор" Style="{StaticResource AwardTitleText}"/>
                        <TextBlock Text="питань: 56" Style="{StaticResource AwardSubText}" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Button Style="{StaticResource AddDeckButton}" Click="CreateTest_Click"/>
            </WrapPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>