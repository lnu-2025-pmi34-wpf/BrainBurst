<UserControl x:Class="BrainBurst.Presentation.Views.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrainBurst.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1180"
             Background="White">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="← Назад"
                Style="{StaticResource BackButton}"
                Click="BackButton_Click"
                Margin="0"/>

        <TextBlock Grid.Row="1" Text="Архів тестів" FontSize="28" FontWeight="Bold" Margin="0,10,0,20"/>

       <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            
            <ItemsControl x:Name="ArchiveItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource ArchiveItemButton}" 
                                Click="ResultItem_Click"
                                Tag="{Binding TestResultId}"> <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Тест: "/>
                                <TextBlock Text="{Binding TestTitle}" Foreground="#555"/>
                                <TextBlock FontWeight="Bold" Margin="10,0,0,0">
                                    <Run Text="{Binding CorrectAnswersPercent, StringFormat='- {0:F0}%'}"/>
                                    <Run Text="{Binding Points, StringFormat='(Бали: {0})'}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding TestDate, StringFormat='{} ({0:dd.MM.yyyy})'}" 
                                           Foreground="Gray" Margin="10,0,0,0"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </ScrollViewer>

    </Grid>
</UserControl>