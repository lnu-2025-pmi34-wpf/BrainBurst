<UserControl x:Class="BrainBurst.Presentation.Views.ArchiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrainBurst.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1180"
             Background="White">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="← Назад"
                Style="{StaticResource BackButton}"
                Click="BackButton_Click"
                Margin="0"/>

        <TextBlock Grid.Row="1" Text="Архів тестів" FontSize="28" FontWeight="Bold" Margin="0,10,0,20"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                
                <Button Style="{StaticResource ArchiveItemButton}" Click="ResultItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Тест: "/>
                        <TextBlock Text="Дискретна математика" Foreground="#555"/>
                        <TextBlock Text=" - 80%" FontWeight="Bold" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ArchiveItemButton}" Click="ResultItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Тест: "/>
                        <TextBlock Text="Психологія" Foreground="#555"/>
                        <TextBlock Text=" - 95%" FontWeight="Bold" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ArchiveItemButton}" Click="ResultItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Тест: "/>
                        <TextBlock Text="Англійська мова" Foreground="#555"/>
                        <TextBlock Text=" - 70%" FontWeight="Bold" Margin="10,0,0,0"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>