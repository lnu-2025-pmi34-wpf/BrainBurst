<UserControl x:Class="BrainBurst.Presentation.Views.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BrainBurst.Presentation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1180"
             Background="White">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="← Назад"
                Style="{StaticResource BackButton}"
                Click="BackButton_Click"
                Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">

            <TextBlock Text="Зміна паролю" FontSize="28" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <TextBlock Text="Старий пароль" FontWeight="Bold" Margin="0,0,0,5" Foreground="#555"/>
            <PasswordBox x:Name="OldPasswordBox" Style="{StaticResource InputPasswordBox}"/>

            <TextBlock Text="Новий пароль" FontWeight="Bold" Margin="0,10,0,5" Foreground="#555"/>
            <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource InputPasswordBox}"/>

            <TextBlock Text="Підтвердіть новий пароль" FontWeight="Bold" Margin="0,10,0,5" Foreground="#555"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Style="{StaticResource InputPasswordBox}"/>

            <TextBlock x:Name="StatusText" Margin="0,10,0,0" FontWeight="SemiBold" 
                       HorizontalAlignment="Center"/>

            <Button Content="Зберегти зміни" 
                    Style="{StaticResource SettingsButtonDelete}"
                    Margin="0,20,0,0"
                    Click="SaveButton_Click"/>

        </StackPanel>
    </Grid>
</UserControl>